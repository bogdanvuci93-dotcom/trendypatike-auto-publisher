{
  "name": "trendypatike-auto-publisher",
  "private": true,
  "type": "module",
  "scripts": {
    "run": "node src/index.mjs",
    "check": "for f in src/*.mjs; do node --check \"$f\" || exit 1; done && node src/selftest.mjs"
  },
  "dependencies": {
    "sharp": "0.34.0"
  }
}
